export type Coordinates = {
    x: number,
    y: number
}

export type Positions = {
    depth: Position,
    layer: Position
}

export type Position = {
    x : number,
    y : number,
    z : number
}
